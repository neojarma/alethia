# Compatibility placeholder for deployment systems that expect backend/.env.
# Alethia uses Next.js API routes and has no separate backend process.
# Configure all runtime secrets in the deployment platform, never in this file.
