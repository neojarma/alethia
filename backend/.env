# Safe local defaults required by Emergent's build-context preparation.
# Production values are supplied through Emergent's environment manager.
MONGO_URL=mongodb://localhost:27017
DB_NAME=alethia
NEXT_INTERNAL_URL=http://127.0.0.1:3000
ALLOWED_ORIGINS=
